# 냉장고를 부탁해 AX — MVP 불편점·개선 결과·개발 백로그

> 작성 기준일: 2026-07-24
>
> 대상 브랜치: `dev_js`
>
> 원칙: 현재 4-Agent MVP 흐름과 화면 구조는 유지하고, 사용성과 결과 신뢰도를 높이는 작은 개선만 반영한다.

## 1. 현재 MVP 한 줄 평가

“유통기한 임박 재료 발견 → 요리 타입 선택 → 레시피 생성·검증 → 소진 재료 보충 구매”라는 핵심 경험은 명확하다. 발표용 MVP로는 충분히 작동하지만, 재고 탐색성·LLM 출력 안정성·날짜 기반 캐시·실사용 개인화가 아직 약하다.

## 2. 이번에 실제 반영한 개선

| 영역 | 기존 불편 | 반영 내용 | 기대 효과 |
|---|---|---|---|
| 전체 냉장고 | 44개 재료를 스크롤만으로 찾아야 함 | 재료명·카테고리 검색 추가 | 원하는 재료를 바로 찾음 |
| 전체 냉장고 | 임박/폐기 상태 개수를 한눈에 알기 어려움 | 전체·임박·폐기 개수와 상태 필터 추가 | 우선 처리할 재료를 빠르게 파악 |
| 레시피 | 생성 결과를 메모·메신저로 옮기기 번거로움 | `레시피 복사` 버튼 추가 | 실제 조리 중 재사용 편의 향상 |
| 장보기 | 플랫폼 미선택으로 버튼이 비활성화된 이유가 약함 | 선택 전 안내와 선택 후 플랫폼 상태 문구 추가 | 다음 행동을 명확히 이해 |
| 접근성 | 선택 버튼의 상태가 시각적 클래스에만 의존 | `aria-pressed` 상태 동기화 | 보조 기술에서 선택 상태 전달 |
| 캐시 | CSV가 같으면 날짜가 바뀌어도 전날 D-day 결과 재사용 가능 | `inventory_date`를 저장하고 날짜 변경 시 자동 무효화 | D-day와 폐기 판정의 날짜 정확성 확보 |
| 레시피 품질 | 형식 오류도 Agent 3 LLM 검증까지 호출 | Python 사전 검증 추가 | 불필요한 호출 감소, 출력 안정성 향상 |
| 프롬프트 | 규칙은 많지만 충돌 시 우선순위가 암묵적 | 안전 → 재고 → 임박 소진 → 타입 → 형식 우선순위 명시 | 규칙 충돌 시 일관된 판단 |
| 프롬프트 | 재고·웹 검색 근거와 지시문 경계가 약함 | `<inventory_data>`, `<untrusted_web_evidence>` 태그로 입력 경계 표시 | 외부 텍스트 오염과 지시 혼동 완화 |
| 프롬프트 | 출력 형태를 설명문으로만 전달 | Agent별 출력 계약과 내부 최종 점검 추가 | 파싱 실패와 섹션 누락 감소 |
| 프롬프트 | 메뉴명 품질 기준이 추상적 | 타입별 좋은/피해야 할 메뉴명 예시 1쌍 추가 | Few-shot으로 타입 정체성 강화 |

### 코드 사전 검증 항목

Agent 2의 레시피가 Agent 3로 넘어가기 전에 다음을 Python이 확인한다.

1. `# 요리 이름` 제목이 있는가
2. `## 재료`, `## 조리법` 섹션이 있는가
3. 조리법이 5~7단계인가
4. D-0~D-5 임박 재료를 최소 1개 사용하는가
5. 유통기한이 지난 재료를 사용하지 않는가

코드로 확실히 판단할 수 있는 조건은 코드가 맡고, 맛의 조화·요리 타입 정체성·실존/유사 조리법 여부처럼 의미 판단이 필요한 조건만 Agent 3가 맡도록 역할을 분리했다.

## 3. 프롬프트 개발 방법

이번 개선은 프롬프트를 길게 만드는 것보다 “역할·입력·우선순위·출력·검증”의 경계를 분명히 하는 데 초점을 맞췄다.

| 방법 | 적용 위치 | 적용 방식 |
|---|---|---|
| Role prompting | Agent 1~4 | 냉장고 관리사, 타입별 요리사, 교차검증자, 장보기 관리사 역할 유지 |
| Instruction hierarchy | Agent 1~4 | 식품 안전과 원본 재고를 스타일·형식보다 먼저 판단하도록 우선순위 명시 |
| Delimiters/XML tags | Agent 1~4 | 재고와 외부 검색 결과를 태그로 감싸 데이터와 지시를 분리 |
| Few-shot contrastive example | Agent 2 | 요리 타입별 좋은 메뉴명/피해야 할 일반적 메뉴명 한 쌍 제공 |
| Output contract | Agent 1~4 | 프런트와 Python이 기대하는 제목·섹션·줄 수를 템플릿으로 제시 |
| Generate → Verify → Retry | Agent 2↔3 | 생성 후 코드 검사와 별도 Agent 검증, 실패 사유를 반영한 1회 재생성 |
| Retrieval-Augmented Verification | Agent 3 | 메뉴명 검색 결과를 근거로 실존·유사 메뉴와 조리 원리 확인 |
| Self-check | Agent 1~4 | 최종 출력 전 누락·중복·제약을 내부 점검하고 최종 결과만 출력 |
| ReAct 응용 | Agent 4 | Observe → Decide → Act로 재고 확인·보충 판단·링크 변환, 중간 추론은 미노출 |
| Deterministic guardrail | Agent 2→3, Agent 4 | 레시피 계약은 Python 검사, 쇼핑 URL은 선택 플랫폼 도메인으로 강제 교정 |

관련 원전·공식 문서:

- [OpenAI Prompt engineering guide](https://developers.openai.com/api/docs/guides/prompt-engineering)
- [OpenAI Structured Outputs guide](https://developers.openai.com/api/docs/guides/structured-outputs)
- [ReAct: Synergizing Reasoning and Acting in Language Models](https://arxiv.org/abs/2210.03629)
- [Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks](https://arxiv.org/abs/2005.11401)
- [Self-Refine: Iterative Refinement with Self-Feedback](https://arxiv.org/abs/2303.17651)

`Structured Outputs`는 현재 CrewAI 마크다운 파이프라인을 크게 바꾸지 않기 위해 이번에는 도입하지 않았다. 현재의 출력 계약 + Python 검사로 MVP를 유지하고, API 계층을 정리하는 다음 단계에서 JSON Schema 적용을 검토하는 것이 안전하다.

## 4. 현재 남아 있는 불편점

### 사용자 경험

| 우선순위 | 불편점 | 영향 |
|---|---|---|
| 높음 | 재고 추가·수정·삭제가 UI에 없고 CSV를 직접 편집해야 함 | 일반 사용자가 자기 냉장고로 쓰기 어려움 |
| 높음 | 알레르기·비선호·식단·인분·조리 가능 시간 입력이 없음 | 개인에게 맞지 않는 레시피가 나올 수 있음 |
| 높음 | Agent 실행 중 단계별 진행률이 없고 전체 로딩만 보임 | 긴 생성 시간을 실패나 멈춤으로 오해 |
| 중간 | 오류가 대부분 브라우저 `alert`로 표시됨 | 오류 원인과 재시도 방법을 놓치기 쉬움 |
| 중간 | 레시피 저장·즐겨찾기·최근 기록이 없음 | 이전에 만족한 결과를 다시 찾기 어려움 |
| 중간 | 모바일에서 긴 레시피/장보기 결과 비교가 불편함 | 실제 주방·매장 사용성이 낮아짐 |
| 낮음 | 검색 결과가 0개일 때 검색어 초기화 버튼이 없음 | 다시 전체 목록으로 돌아가는 동작이 한 단계 더 필요 |

### 기능·데이터

| 우선순위 | 개발 필요점 | 현재 위험 |
|---|---|---|
| 높음 | 레시피 사용량과 재고 단위의 정규화 계산 | `반 모`, `1대`, `g`, `ml` 등 단위가 섞여 요리 후 잔량이 LLM 추정에 의존 |
| 높음 | 샘플 CSV의 절대 유통기한 갱신 또는 상대 날짜 fixture | 시간이 지나면 대부분 폐기 재료가 되어 데모가 무너질 수 있음 |
| 높음 | 사용자별 세션/저장소 분리 | 현재 서버 상태와 캐시가 전역이라 여러 사용자를 지원하지 못함 |
| 중간 | 장보기 가격·재고·배송 가능 여부 확인 | 현재 링크는 단순 검색 링크이며 실제 구매 가능성을 보장하지 않음 |
| 중간 | 검색 출처 품질 평가와 허용 도메인 | DDGS 검색 결과의 신뢰도 편차가 큼 |
| 중간 | 영양 정보·알레르기 교차 확인 | 레시피가 건강·안전 요구를 충분히 검증하지 않음 |
| 낮음 | 추천 타입 점수/근거 표시 | 현재는 추천 타입 한 개만 노출되어 대안 비교가 어려움 |

### 기술·운영

| 우선순위 | 개발 필요점 | 현재 위험 |
|---|---|---|
| 높음 | 고정 평가셋과 자동 eval | 프롬프트 변경이 실제 품질을 높였는지 회귀 판단 불가 |
| 높음 | 단위 테스트·API 통합 테스트·브라우저 테스트 | CSV/Markdown/캐시 변경 시 회귀를 수동으로만 발견 |
| 높음 | 요청 timeout·취소·재시도 정책 | 외부 검색이나 LLM 지연 시 사용자가 오래 대기 |
| 중간 | 작업 큐 또는 요청별 실행 상태 | 전역 lock 때문에 한 작업이 길면 다른 요청도 대기 |
| 중간 | 구조화 로그·호출 시간·토큰·비용 지표 | 느린 Agent와 비용 증가 원인을 찾기 어려움 |
| 중간 | 프롬프트 버전과 결과 평가 기록 | 어떤 프롬프트가 어떤 결과를 냈는지 비교하기 어려움 |
| 중간 | 배포 전 인증·rate limit·CSRF/CORS 검토 | 현재 로컬 데모 서버를 그대로 외부 공개하면 안 됨 |

## 5. 추천 개발 순서

### P0 — MVP 안정화

1. 10~20개 고정 냉장고 시나리오로 프롬프트 eval 작성
2. CSV 날짜 fixture를 실행일 기준으로 생성하는 데모 모드 추가
3. 수량·단위 파서와 요리 후 잔량 계산을 Python으로 이동
4. Agent timeout, 오류 카드, 재시도 버튼 추가
5. 핵심 함수와 API에 자동 테스트 추가

완료 기준: 같은 입력에서 폐기 재료 사용 0건, 임박 재료 누락 0건, 형식 파싱 성공률 95% 이상.

### P1 — 실제 사용자 입력

1. 재고 추가·수정·삭제 UI
2. 인분·조리 시간·알레르기·비선호 입력
3. 최근 레시피와 즐겨찾기
4. 생성 단계 표시: 재고 분석 → 레시피 생성 → 웹 검증 → 완료

완료 기준: CSV를 직접 열지 않고 한 사용자가 자신의 냉장고 데이터로 전체 흐름을 완료.

### P2 — 서비스 확장

1. 사용자별 DB와 로그인
2. 커머스 공식 API/제휴 링크, 가격·재고 비교
3. 영양/알레르기 데이터 소스 연동
4. 운영 모니터링, 비용 제한, rate limit
5. 모바일 사용성·접근성 정식 점검

완료 기준: 여러 사용자의 데이터가 분리되고, 외부 서비스 실패에도 핵심 냉장고 기능이 유지.

## 6. MVP에서 의도적으로 하지 않은 것

- 프레임워크 전환 또는 화면 전체 재설계
- Agent 수와 기본 순서 변경
- CSV를 DB로 전면 교체
- CrewAI 제거
- 유료 검색/쇼핑 API 도입
- 기존 다크 메탈 테마 변경
- 근거 없이 프롬프트를 전부 새로 작성

이번 변경은 현재 MVP의 정체성을 고정한 채, 탐색성·안정성·프롬프트 준수율을 높이는 범위로 제한했다.

## 7. 수동 확인 시나리오

1. 홈에서 `고등어`를 검색하면 해당 카드만 보이는지 확인
2. `임박`, `폐기`, `전체` 필터의 개수와 카드 상태가 일치하는지 확인
3. 카테고리명 `채소`로도 검색되는지 확인
4. 플랫폼 미선택 시 장보기 버튼이 비활성화되고 안내 문구가 보이는지 확인
5. 쿠팡/컬리 선택이 홈과 레시피 화면에 같이 반영되는지 확인
6. 생성된 레시피의 복사 버튼이 동작하고 완료 상태가 표시되는지 확인
7. 레시피가 제목·조리 정보·재료·5~7단계 조리법을 갖는지 확인
8. 폐기 재료를 넣은 테스트 레시피가 코드 사전 검증에서 탈락하는지 확인
9. 날짜가 바뀐 캐시가 서버 시작 시 무효화되는지 확인
10. 정적 모드에서 기준일이 지난 `nbax_data.js`를 최신 데이터로 오인하지 않는지 확인

## 8. 브랜치 안전 수칙

- 작업 브랜치: `dev_js`
- `main`에서 직접 커밋하거나 푸시하지 않는다.
- 푸시 직전 `git branch --show-current`가 `dev_js`인지 다시 확인한다.
- 원격 대상은 `origin/dev_js`로 명시한다.
- `main` 반영은 별도 검토와 Pull Request를 통해서만 진행한다.
